# 🚀 ULTIMATE SERVER STRESS TESTER

**Версия 2.0 (Optimized Edition)**

Высокопроизводительный инструмент для нагрузочного тестирования UDP серверов, написанный на Go.

---

## 📋 Содержание

- [Запуск](#-запуск)
- [Режимы тестирования](#-режимы-тестирования)
- [Рекомендации](#-рекомендации)
- [Параметры командной строки](#️-параметры-командной-строки)
- [Примеры использования](#-примеры-использования)
- [Компиляция](#-компиляция)
- [Оптимизации](#-оптимизации)
- [Статистика](#-статистика)
- [Важная информация](#️-важная-информация)
- [Системные требования](#️-системные-требования)

---

## 🚀 Запуск

### 1. Интерактивный режим (рекомендуется)

```bash
server_stresser.exe
```

### 2. С параметрами командной строки

```bash
server_stresser.exe -mode 1 -duration 60 -goroutines 2000
```

---

## 📋 Режимы тестирования

### Режим 1: EXTREME UDP FLOOD
**~500K-1M req/s** - Оптимизированная отправка
- ✅ Непрерывная отправка с батчингом и пулом соединений
- 📊 Рекомендовано: Горутин = CPU × 50-100

### Режим 2: RAW SPEED FLOOD
**~1M-2M req/s** - Максимальная скорость
- ⚡ Zero-copy syscalls для максимальной производительности
- 📊 Рекомендовано: Горутин = CPU × 200-500

### Режим 3: AMPLIFICATION ATTACK
**~50K-300K req/s** - Тест усиления трафика
- 📡 Запросы с получением больших ответов (усиление bandwidth)
- 📊 Рекомендовано: Горутин = CPU × 20-50

### Режим 4: MIXED SIZE ATTACK
**~800K-1.5M req/s** - Обход фильтров
- 🔀 Пакеты разных размеров (100-1400 байт)
- 📊 Рекомендовано: Горутин = CPU × 50-100

### Режим 5: COORDINATED STRIKE
**~1M-2M+ req/s** - Использует все CPU
- 🎯 Координированное использование ВСЕХ ядер процессора
- 📊 Рекомендовано: Горутин на ядро = 100-200

### Режим 6: BURST ATTACK
**Зависит от N** - Быстрая отправка N запросов
- 💥 Отправка заданного количества запросов максимально быстро
- 📊 Рекомендовано: Горутин = CPU × 100

### Режим 7: ADAPTIVE LOAD 🆕
**Автоподстройка** - Умная нагрузка
- 🧠 Автоматическая подстройка под производительность системы
- 📊 Рекомендовано: Начальных горутин = CPU × 50

---

## 💡 Рекомендации

### Первый раз (тестовая нагрузка)
- **Режим 1** → 30 секунд → CPU × 75 горутин

```bash
server_stresser.exe -mode 1 -duration 30 -goroutines 600
```

### Средняя нагрузка
- **Режим 1** → 60 секунд → CPU × 150 горутин

```bash
server_stresser.exe -mode 1 -duration 60 -goroutines 1200
```

### Максимальная мощность
- **Режим 2** → 120 секунд → CPU × 300 горутин

```bash
server_stresser.exe -mode 2 -duration 120 -goroutines 2400
```

### Умная адаптация
- **Режим 7** → 300 секунд → CPU × 50 начальных горутин

```bash
server_stresser.exe -mode 7 -duration 300 -goroutines 400
```

---

## ⚙️ Параметры командной строки

### Базовые параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `-ip` | string | IP адрес сервера | `"185.189.255.121"` |
| `-port` | int | Порт сервера | `25015` |
| `-mode` | int | Режим атаки (1-7) | `1` |
| `-duration` | int | Длительность в секундах | `60` |
| `-goroutines` | int | Количество горутин | `1000` |

### Продвинутые параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `-batch` | int | Размер батча для отправки | `100` |
| `-pool-size` | int | Размер пула соединений | `100` |
| `-threads-per-core` | int | Горутин на ядро CPU (режим 5) | `100` |
| `-requests` | int | Всего запросов (режим 6) | `1000000` |
| `-adaptive` | bool | Адаптивная подстройка нагрузки | `false` |
| `-detailed` | bool | Детальная статистика | `true` |

---

## 📊 Примеры использования

### Интерактивный режим с полным контролем

```bash
server_stresser.exe
```

### Быстрый тест - режим 1, 60 сек, 2000 горутин

```bash
server_stresser.exe -mode 1 -duration 60 -goroutines 2000
```

### Максимальная скорость - режим 2 с zero-copy

```bash
server_stresser.exe -mode 2 -duration 30 -goroutines 5000 -batch 500
```

### Использовать все ядра CPU - режим 5

```bash
server_stresser.exe -mode 5 -duration 120 -threads-per-core 150
```

### Отправить миллион запросов - режим 6

```bash
server_stresser.exe -mode 6 -requests 1000000 -goroutines 1000
```

### Адаптивный режим с самоподстройкой - режим 7

```bash
server_stresser.exe -mode 7 -duration 300 -goroutines 500
```

### Кастомный IP и порт

```bash
server_stresser.exe -ip 192.168.1.100 -port 27015 -mode 1 -duration 60
```

### Оптимизированная конфигурация с батчингом

```bash
server_stresser.exe -mode 1 -duration 60 -goroutines 2000 -batch 200 -pool-size 200
```

---

## 🔧 Компиляция

### Стандартная компиляция

```bash
go build -o server_stresser.exe server_stresser.go
```

### Оптимизированная (меньший размер)

```bash
go build -ldflags="-s -w" -o server_stresser.exe server_stresser.go
```

### С дополнительной оптимизацией

```bash
go build -ldflags="-s -w" -trimpath -o server_stresser.exe server_stresser.go
```

---

## ⚡ Оптимизации

Программа включает множество оптимизаций для максимальной производительности:

- ✅ **Пулинг UDP соединений** - переиспользование соединений
- ✅ **Батчинг отправки** - группировка пакетов
- ✅ **Zero-copy syscalls** - прямые системные вызовы без копирования
- ✅ **Cache line padding** - избежание false sharing между CPU
- ✅ **Пулинг памяти** - переиспользование буферов пакетов
- ✅ **Локальные счетчики** - минимизация атомарных операций
- ✅ **Предаллоцированные пакеты** - пакеты создаются один раз

---

## 📈 Статистика

Программа выводит детальную статистику в реальном времени:

- 📤 **Отправлено запросов** - общее количество отправленных пакетов
- 📥 **Получено ответов** - количество полученных ответов от сервера
- 💾 **Объем полученных данных** - размер данных в байтах/KB/MB
- ❌ **Количество ошибок** - неудачные попытки отправки
- 📊 **Средняя скорость отправки** - requests/second
- 🚀 **Пиковая скорость** - максимальная мгновенная скорость
- 📡 **Коэффициент усиления** - для режима 3 (Amplification)
- 💻 **Использование памяти** - потребление RAM
- 🔢 **Количество горутин** - активные воркеры
- 🔧 **Системные вызовы** - для режима 2 (Zero-copy)

---

## ⚠️ Важная информация

### ✅ Этот инструмент предназначен ТОЛЬКО для:

- 🔒 Тестирования собственных серверов
- 📝 Легального нагрузочного тестирования
- 🎓 Образовательных целей с разрешением

### ❌ ЗАПРЕЩЕНО использовать для:

- 🚫 Атак на чужие серверы без разрешения
- 🚫 Нарушения работы сервисов
- 🚫 Любой незаконной деятельности

### ⚖️ Юридическая ответственность

**Ответственность за использование лежит на пользователе.**

Использование данного инструмента для атак на инфраструктуру без явного разрешения владельца является незаконным и может повлечь за собой уголовное преследование согласно законодательству вашей страны.

---

## 🛠️ Системные требования

- **ОС:** Windows (для .exe) или любая ОС с Go для компиляции
- **RAM:** Минимум 2 ГБ (рекомендуется 4+ ГБ)
- **CPU:** Многоядерный процессор (для максимальной производительности)
- **Сеть:** Стабильное сетевое подключение

---

## 📝 История версий

### Версия 2.0 (Optimized Edition)

**Новые возможности:**
- ➕ Добавлен режим 7 (Adaptive Load)
- 🖥️ Интерактивное меню конфигурации
- 🚩 Флаги командной строки для всех параметров
- ⚡ Оптимизация производительности (пулинг, батчинг, zero-copy)
- 📊 Детальная статистика в реальном времени
- 🎨 Цветной вывод в терминал
- 📈 Пиковые значения скорости
- 💡 Автоматические рекомендации по параметрам

---

## 📄 Лицензия

Этот проект предназначен исключительно для легального тестирования производительности и образовательных целей.

---

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы с использованием инструмента, убедитесь, что:

1. Вы используете актуальную версию Go (1.16+)
2. У вас есть права на тестирование целевого сервера
3. Параметры запуска соответствуют вашему оборудованию

---

**Сделано для лохов**
